# Publication Android

`app-release.apk` permet une installation directe sur Android.

`app-release.aab` est destiné à Google Play.

Pour une publication officielle, il faut configurer la signature Android et le compte Google Play Console. Ne pas placer une clé privée de signature dans un dépôt GitHub public.
